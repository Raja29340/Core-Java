import java.util.ArrayList;
import java.util.List;
import java.util.ListIterator;

class HelloWorld {
    public static void main(String[] args) {
        List l = new ArrayList(); // Specify type parameter for the list
        l.add(10);
        l.add("Rajan");
        l.add(null);
        l.add('R');
        l.add(12.4);
        
        ListIterator s1 = l.listIterator(); // Corrected method call

        // Print elements using the ListIterator
        while (s1.hasNext()) {
            System.out.println(s1.next());
        }
    }
}
